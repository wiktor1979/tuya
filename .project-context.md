# Projekt: Tuya Heat Pump Monitor
## Lokalizacja: C:\tuya
## Repo: GitHub (git skonfigurowany, remote istnieje)
## Deploy: Fly.io (app: 'scop', region: arn/ams, 1GB RAM, shared CPU)

## Opis
Aplikacja monitorująca pompę ciepła przez Tuya Pulsar (EU). Dwa procesy:
- main.py — zbieracz danych z Tuya + wątek pogodowy (Open-Meteo co 1h)
- dashboard.py — Streamlit dashboard (port 8501)

## Stack technologiczny
- Python 3.11 (Dockerfile: python:3.11-slim)
- Streamlit + Plotly (dashboard)
- SQLite z WAL, connection pooling, indeksy (baza: /data/tuya_telemetry.db na woluminie Fly.io)
- Tuya Pulsar + PyCryptodome (AES-GCM/ECB deszyfrowanie)
- streamlit-autorefresh (dynamiczny interwał: 60s gdy pompa pracuje, 300s gdy stoi)

## Struktura po refaktoryzacji (2026-08-25)
- dashboard.py (142 linii) — orkiestrator (strona główna multipage)
- pages/2_Analiza_Parametrow.py — podstrona: Analiza Parametrów (5 zakładek: COP, Hydraulika ΔT, Sprężarka/Taktowanie, Defrost/EEV, Krzywa Grzewcza)
- app/services/data_loader.py — zapytania SQL, przetwarzanie danych, obliczenia COP/SCOP
- app/services/database.py — CRUD, init_db, save_properties_to_db, weather, settings, fault_log
- app/services/tuya_client.py — TuyaPulsarClient, MultiAccountTuyaClient, DeadbandFilter (z NO_HEARTBEAT_CODES)
- app/services/analytics.py — diagnostyka: short cycles, inverter analysis, weather correlation, heating curve advisor, decode_fault_bitmap
- app/services/exporter.py — eksport CSV
- app/ui/styles.py — CSS, re-export PARAM_INFO z config.py
- app/ui/sidebar.py — SidebarSettings dataclass, render_sidebar()
- app/ui/tab_main.py — Panel Główny
- app/ui/tab_scop.py — Bilans Energetyczny & SCOP
- app/ui/tab_diagnostics.py — Diagnostyka (taktowanie >12 startów/dzień)
- app/ui/tab_weather.py — Kontekst Pogodowy
- app/ui/tab_meter.py — Fizyczny Licznik Energii (ręczne wpisy)
- app/ui/tab_export.py — Eksport CSV
- app/ui/tab_heating_curve.py — Doradca Krzywej Grzewczej (formularz, analiza duty cycle, rekomendacje)
- app/config.py — env vars, HISTERESIS_CONFIG, TEMP_CODES, PARAM_INFO (46 wpisów), FAULT_BITMAP_LABELS
- db.py — warstwa kompatybilności wstecznej
- main.py — collector + weather thread + save_with_fault_detection

## Kluczowe parametry pompy
- Device ID: bf874f7ae72aca1fc23op0
- Model Tuya: 0000043th5
- Lokalizacja: Łódź (51.7592, 19.4560)
- Kody temperatur (dzielone /10): in_water_temp, out_water_temp, tank_temp, amb_temp, disc_temp, back_temp, tidr, heat_temp_set_z2, cool_temp_set_z2, auto_heat_temp_set_z1/z2, auto_cool_temp_set_z2, idr_temp_set
- Parametry chłodnicze: m_eev (główny zawór EEV, 0-480 kroków), a_eev (dodatkowy EEV), dc_fan1 (wentylator DC, 0-1000 RPM)
- Flagi binarne: defrost, freeze, fault_flag, protect_flag, pump_sta, valve, switch
- Tryb pracy: valve >= 0.5 = CWU, < 0.5 = CO
- Sprężarka pracuje gdy comp_freq > 5
- Strefy grzewcze: zone_select (0=brak, 1=Z1, 2=Z2, 3=obie), heat_temp_set=Z1 (41°C), heat_temp_set_z2=Z2 (28°C podłogówka)
- work_mode: cool, heat, auto, hot_water, cool_hot_water, heat_hot_water, auto_dhw
- fault: bitmapa 30 bitów — E01-E16 (bit 0-15), P01-P14 (bit 16-29)
- tidr: temperatura pokojowa (czujnik wewnętrzny), NIE temperatura ssania

## Histereza (HISTERESIS_CONFIG)
- tank_temp: active=2 (0.2°C), idle=5 (0.5°C)  # zmienione z 3 na 2 (lepsza krzywa CWU)
- out/in_water_temp: active=2, idle=5
- amb_temp: active=5, idle=8
- disc_temp/back_temp: active=5, idle=15
- ac_vol: active=2.0, idle=3.0  # zmienione z 3.0/5.0 (dokładniejsze P_el)
- MAX_HEARTBEAT_SEC = 300 (wymuszony zapis co 5 min)
- NO_HEARTBEAT_CODES: ac_fan, dc_fan2, defrost, fault_flag, freeze, protect_flag, pump_sta, valve (tylko przy zmianie wartości)

## Wykonane naprawy (2026-08-25)
1. .gitignore — naprawiony (był uszkodzony)
2. SQL injection — f-stringi zamienione na parametryzowane zapytania
3. Zbędne load_dotenv/sys.path.insert usunięte z main.py
4. Autorefresh — naprawiony (był bug: zapytanie do nieistniejącej kolumny compressor_status)
5. Refaktoryzacja dashboard.py z ~1250 na 142 linii (podział na moduły)
6. use_container_width=True → width="stretch"
7. Taktowanie: próg zmieniony na >12 startów/dzień (relatywny do zakresu)
8. README zaktualizowany

## Zrealizowane (2026-08-26)
- Podstrona "Analiza Parametrów" z 4 zakładkami monitoringu wg kategorii z opis.txt
- Dodano statsmodels do requirements.txt (dla trendline lowess w Plotly)
- data_loader rozszerzony o: m_eev, a_eev, dc_fan1, freeze

## Zrealizowane (2026-08-26 wieczór / 2026-08-27)
### Auto-odświeżanie i countdown
- st_autorefresh: stały key ("auto_refresher" / "analysis_refresher") zamiast zmiennego — zapobiega resetowaniu timera
- st_autorefresh przeniesiony na górę strony (przed title/sidebar/button)
- Countdown JavaScript w st.html() (NIE st.components.v1.html — deprecated)
- count z st_autorefresh w komentarzu HTML <!-- render:{count} --> wymusza re-render iframe po każdym odświeżeniu

### SCOP realny z defrostem
- P_th_kw_raw — surowa moc cieplna (zachowuje ujemne wartości)
- P_th_defrost_kw — strata cieplna defrostu (< 0 gdy defrost=True i ΔT < 0)
- E_th_defrost_kwh / E_el_defrost_kwh — energia defrostu
- SCOP realny = (E_th_nominal + E_th_defrost) / E_el_total
- compute_scop_metrics zwraca: scop_total (nominalny), scop_real, e_th_defrost, e_el_defrost, defrost_loss_pct
- compute_daily_stats: SCOP_realny dzienny, agregacja strat defrostu
- tab_scop.py: 4 KPI (nominalny, realny, CO, CWU), sekcja strat defrostu, tabela z SCOP nom./real.

### Próg opłacalności SCOP 3.1
- Linia referencyjna na wykresie COP chwilowy (pomarańczowa)
- KPI "SCOP Realny" z oceną vs próg 3.1 (zielony ✓ / czerwony ✗)
- Wykres SCOP dzienny: słupki SCOP realny (kolorowane zielono/czerwono wg progu), linia SCOP nominalny
- Filtr: dni bez pracy sprężarki (dt_hours_work == 0) wykluczane z wykresu i alertów
- Tekst: "mniej opłacalna niż ogrzewanie gazowe" (nie elektryczne)

### Alerty wydajności
- Zmienione z COP chwilowego na SCOP dzienny realny
- Filtr: dt_hours_work > 0 (pompa musiała grzać)
- Severity: 🚨 error gdy SCOP < 2.5, ⚠️ warning gdy 2.5-3.1
- Kontekst: data, SCOP, śr. temp. zewn., liczba defrostów

### Histereza deadband — optymalizacja
- tank_temp active: 3 → 2 (0.3°C → 0.2°C) — lepsza krzywa grzania CWU
- ac_vol: 3.0/5.0 → 2.0/3.0 — dokładniejsze P_el

### UI
- Tytuły: st.title() → kompaktowy <h3> z min marginesem
- CSS: padding-top: 1rem, header height: 2rem (redukcja pustej przestrzeni)
- Usunięty wykres "Chwilowe COP z podziałem na tryb CO/CWU" z Panelu Głównego (jest na Analizie)
- Bilans dobowy zastąpiony wykresem SCOP dziennego z progiem opłacalności

### WAŻNE zasady (zapamiętane z feedbacku użytkownika)
- NIE używać st.components.v1.html — jest deprecated, używać st.html()
- Próg opłacalności SCOP = 3.1 (porównanie z ogrzewaniem gazowym, nie elektrycznym)

## Zrealizowane (2026-08-27 wieczór)
### Panel Główny
- Kafelek "🎯 Zadana CWU" (hot_water_temp_set) obok "Woda CWU" — 7 kolumn zamiast 6
- Alerty trybów specjalnych: 🏖️ Holiday (holiday_sw) i 🔇 Silent (mute) — widoczne tylko gdy aktywne
- hot_water_temp_set dodany do NEEDED_COLS i RESAMPLE_AGG w data_loader

### Zakresy czasu (sidebar)
- Usunięty zakres "Ostatnie 6 godzin"
- "1 dzień" → "Dzisiaj" (definicja bez zmian — dane od 00:00)
- Dodany zakres "Ostatnie 90 dni" (2160h)
- Pogoda: dynamiczne days=max(30, hours_back//24+1) zamiast hardcoded 30

### Porządki techniczne
- calculator.py usunięty (potwierdzone zero importów)
- PARAM_INFO — jedno źródło prawdy w config.py; styles.py re-eksportuje
- Poprawiona literówka "gazy" → "gazu" w PARAM_INFO (disc_temp)
- Dodane brakujące opisy: a_eev, dc_fan1, freeze (PARAM_INFO: 17 → 20 wpisów)
- __pycache__ wyczyszczone lokalnie

### Skrypt start_local.bat
- Pobiera bazę z Fly.io (sftp get) z rotacją .old
- Pyta czy nadpisać bazę jeśli istnieje lokalnie
- Auto-migracja init_db() po pobraniu
- Uruchamia Streamlit (Panel.py) na porcie 8501

## Zrealizowane (2026-08-27 noc)
### Doradca Krzywej Grzewczej
- Nowa zakładka "📈 Krzywa Grzewcza" w Analizie Parametrów (5. zakładka)
- app/ui/tab_heating_curve.py — formularz krzywej (2 punkty: -10°C i +20°C), tabela binów, rekomendacja
- analyze_heating_curve() w analytics.py — duty cycle sprężarki per bin temp (tylko CO)
- Wykrywanie przyczyny zatrzymania: termostat pokojowy (out < set) vs pompa osiągnęła cel
- Korelacja z nasłonecznieniem — adnotacja ☀️ gdy zyski solarne wpływają na duty cycle
- Wykrywanie poza-sezonu (duty cycle < 5% → brak fałszywych rekomendacji)
- Rekomendacja: konkretne wartości do zmiany lewego/prawego końca krzywej

### Baza danych
- Tabela settings (key/value) — persystencja ustawień użytkownika (get_setting/set_setting)
- weather_data: kolumny direct_radiation, diffuse_radiation (migracja ALTER TABLE)
- init_db() obsługuje migrację automatycznie — bezpieczne dla istniejących baz

### Open-Meteo nasłonecznienie
- Collector (main.py) pobiera direct_radiation i diffuse_radiation
- Panel.py obsługuje dynamiczną liczbę kolumn weather_df

### Porządki techniczne
- __pycache__ usunięty z git (git rm -r --cached)
- .gitignore naprawiony (merge conflict markers)
- any → Any w typach (config.py, tuya_client.py — 4 wystąpienia)

## Zrealizowane (2026-08-28)
### Obsługa dwóch stref grzewczych
- heat_temp_set_z2, zone_select, work_mode dodane do NEEDED_COLS/RESAMPLE_AGG w data_loader
- zone_select i work_mode dołączane jako val_str przez merge_asof (parametry stringowe)
- Korekta historycznych wartości heat_temp_set_z2 (350→35.0, przed dodaniem do TEMP_CODES)
- Kafelek nastawy CO reaguje na zone_select: Z1→Nastawa CO, Z2→Nastawa Z2, Z3→Nastawa Z1+Z2
- Krzywa grzewcza: analyze_heating_curve() pomija okresy z zone_select=2 (podłogówka, stała nastawa)
- Logika stref: zone_select 0=brak, 1=Z1, 2=Z2, 3=obie; heat_temp_set=Z1 (41°C), heat_temp_set_z2=Z2 (28°C)

### Ikona trybu pracy w sidebarze
- get_current_work_mode() w database.py — pobiera ostatni work_mode
- Duża ikona na dole panelu bocznego: 🔥=heat, 🚿=hot_water, 🔥🚿=heat_hot_water, ❄️=cool, 🔄=auto itd.
- Pełna lista trybów z oficjalnej specyfikacji: cool, heat, auto, hot_water, cool_hot_water, heat_hot_water, auto_dhw

### Optymalizacja DeadbandFilter
- NO_HEARTBEAT_CODES: ac_fan, dc_fan2, defrost, fault_flag, freeze, protect_flag, pump_sta, valve
- Flagi binarne nie mają heartbeatu co 5 min — zapisywane tylko przy zmianie wartości
- Eliminuje ~25k zbędnych rekordów/dobę

### Obsługa błędów pompy (fault)
- decode_fault_bitmap() w analytics.py — parsuje bitmapę fault na kody E01-E16, P01-P14
- Tabela fault_log w bazie: id, timestamp, device_id, fault_code, fault_bitmap, resolved, resolved_at
- Funkcje CRUD: log_fault(), resolve_faults(), get_active_faults(), get_fault_history()
- save_with_fault_detection() wrapper w main.py — loguje awarie w czasie rzeczywistym
- Czerwony banner 🚨 na Panelu Głównym gdy aktywne kody błędów
- Sekcja "Historia Awarii" w Diagnostyce — aktywne awarie + tabela historii z czasem trwania
- FAULT_BITMAP_LABELS w config.py — mapowanie 30 bitów na kody błędów

### Oficjalna specyfikacja modelu Tuya (model 0000043th5)
- PARAM_INFO rozszerzony z 23 do 46 wpisów — pełne opisy wszystkich parametrów
- TEMP_CODES rozszerzony o: heat_temp_set_z2, cool_temp_set_z2, auto_heat_temp_set_z1/z2, auto_cool_temp_set_z2, idr_temp_set
- Poprawki: tidr = temp. pokojowa (nie ssania), valve = zawór 3-drożny
- work_mode: 7 trybów (cool, heat, auto, hot_water, cool_hot_water, heat_hot_water, auto_dhw)
- fault: bitmapa 30 bitów (E01-E16 + P01-P14)
- use_container_width=True → width="stretch" (ostatnie wystąpienie w tab_heating_curve.py)

### WAŻNE zasady (uzupełnione)
- Wszystkie parametry z pompy zapisywane do bazy (brak whitelisty) — nieznane parametry też trafiają do telemetry
- zone_select jest val_str (nie val_num) — wymaga konwersji
- Historyczne heat_temp_set_z2 w bazie mają surowe wartości (350 zamiast 35.0) — korekta >100 → /10

## Znane pozostałe problemy
- Brak alertów/powiadomień (Telegram/email)
- Brak wykrywania utraty komunikacji
- Brak backupu bazy SQLite
- requirements.txt bez pinów wersji

## Planowane usprawnienia (priorytet)
1. Alerty Telegram przy awariach
2. Wykrywanie utraty komunikacji
3. Trend degradacji wydajności (SCOP MoM)
4. Heatmapa aktywności
5. Backup bazy
